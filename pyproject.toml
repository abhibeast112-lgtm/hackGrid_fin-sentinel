[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fin-sentinel"
version = "0.1.0"
description = "Financial investigation engine"
requires-python = ">=3.13"

[tool.setuptools.packages.find]
where = ["backend"]
